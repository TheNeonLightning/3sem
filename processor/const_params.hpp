const int PROGRAM_MIN_SIZE = 10;

const int MAX_COMMAND_SIZE = 10;

const int COMMANDS_QUANTITY = 22; /// Number of existing commands.

const int COMPLEX_PARSING_CMD = 98; /// the commands with code above or equal to it
                                    /// are parsed as complex ones: it includes
                                    /// some additional actions besides saving
                                    /// code in program array
